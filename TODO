TODO

* parse request body more nicely (currently, it cannot handle spaces properly)
* store state (e.g. base-url and headers) to local file system

DONE

* "status" command
* enable to exec command with args as an array
* enable to set headers
